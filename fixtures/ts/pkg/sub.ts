import { Bus } from "./bus";
import { events } from "./bus";

function subscribe(bus: Bus): BroadcastChannel {
  decode();
  return events;
}

function decode(): void {
  handle();
}

function handle(): void {}
